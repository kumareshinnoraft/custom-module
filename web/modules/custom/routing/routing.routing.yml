routing.example:
  path: '/routing'
  defaults:
    _title: 'Example'
    _controller: '\Drupal\routing\Controller\RoutingController::build'
  requirements:
    # Only who have access the permission of 'access the custom page'. 
    _permission: 'access the custom page'
    